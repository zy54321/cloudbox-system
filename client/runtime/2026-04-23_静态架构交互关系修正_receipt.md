# 回执：静态架构交互关系修正（links + 聚焦 + 面板 + 材质）

**日期**：2026-04-23

## 目标

- 重组 **`public/config/links.json`**：删除 `link-info-2`～`5`，新增 **`link-info-hts-concurrent`**、**`link-info-ground-concurrent`**；全量 **`transfers`**；两条新关系 **`focusGroups`**；**`relations` 顺序**与面板及完整播放一致（info-1 → HTS 并发 → 地面并发 → info-6 → ctrl-*）。
- **`CesiumViewport.vue`**：缓存 **`RELATION_FOCUS_GROUPS`**；**`playRelationFocusGroupsSequence`** / **`playRelationFocusUnified`**；**`visibleRelationId`** 与多关系播放走统一入口；**`splitMode=false`** 时链路单色冷暖材质。
- **`StaticHome.vue`**：关系行冷暖 class、选中展开 **`transfers`**；**`playAllRelationsSequence`** **`await playRelationFocusUnified`**。
- **`proto.css`**：静态侧关系卡片与详情区冷暖样式。

## 实际修改文件

- `public/config/links.json`
- `src/components/CesiumViewport.vue`
- `src/pages/StaticHome.vue`
- `src/styles/proto.css`
- `runtime/TASK_CURRENT.md`
- `runtime/2026-04-23_静态架构交互关系修正_receipt.md`（本文件）

## 关键 diff 摘要

- **links.json**：并发边与 cam/viewPreset；**`transfers`** 与 **edges** 一致；**`focusGroups`** 仅两条新链必填级配置。
- **CesiumViewport**：加载链路时写入 **`RELATION_FOCUS_GROUPS`**，clear 时同步清理；**`playRelationFocusUnified`** 在存在 **`focusGroups`** 时走组序列，否则走原节点序列；静态单屏 **`getLinkMaterialByFlowLabel`** 使用单色材质工厂，**`splitMode`** 仍为分屏 Shader。
- **StaticHome**：**`cb-relation--info` / `cb-relation--ctrl`**；详情 **`transfers`** 列表；完整播放以 unified 序列时长为准，条间短间隔。
- **proto.css**：**`.cb-wrap--static`** 下关系卡片、徽标、详情与列表的冷/暖区分。

## 刻意未做项 / 风险点

- **动态/compare** 仍使用 **`engine_failure_links.json`** 等；若缺 **`focusGroups`** 自动回落 **`playRelationNodeFocusSequence`**，不依赖本静态 JSON 新字段。
- **完整播放** 为长 **`await`** 链，需用户侧验证快速取消/切页时 **`_relationFocusSeq`** / **`_relationSequenceToken`** 与 UI 仍一致。
- **未**在回执中要求或记录自动化测试执行。

---

## 增量（同日收口：并发聚焦细节）

**修改文件**：`src/components/CesiumViewport.vue`、`runtime/TASK_CURRENT.md`、本回执。

- **`focusRelationNode`**：非飞机节点在 **`flyCameraToUnitId`** 前 **`clearMarkerPopupFromRelationFocus()`** + **`waitMs(80)`**（含 seq 校验），飞机分支未改。
- **`flyCameraToUnitIdsBoundingSphere`**：可选 **`opts.rangeScale`**、**`opts.minRange`**、**`opts.pitchDeg`**，默认与原先 **`2.8` / `25000` / `-35°`** 一致。
- **`playRelationFocusGroupsSequence`**：仅 **group** 步传入收紧的包围球参数（初版 **`1.9` / `14000` / `-32`**，后在「group 多 popup」增量中改为 **`1.6` / `10000` / `-30`**，见下节）。
- **`TASK_CURRENT.md`**：补充「今日收口」两条说明（popup 时机、group 镜头），明确为静态架构细节优化、非主链路重构。

---

## 增量：group 多 popup + 镜头再收紧（展示层）

**涉及文件**：`src/components/CesiumViewport.vue`、`src/pages/StaticHome.vue`、`runtime/TASK_CURRENT.md`、本回执。

- **事件链**：`defineEmits` 增加 **`marker-group-show` / `marker-group-move` / `marker-group-close`**；**`activeMarkerEntities`** 专用于 group 跟随，与 **`activeMarkerEntity`** 分离；**`emitMarkerPopupGroupForUnitIds`**、**`clearMarkerPopupGroupFromRelationFocus`**（不触发 **`marker-close`**）。
- **聚焦序列**：**`playRelationFocusGroupsSequence`** 每步开始前 **`clearMarkerPopupGroupFromRelationFocus`**；group 步在 **`waitMs(1100)`** 后 **`emitMarkerPopupGroupForUnitIds`**，再 **`flashUnitBillboards`**；收尾/切关系前清理 group；**`emitMarkerPopupForUnitId`** 与地图拾取前清理 group，避免与单点混叠。
- **`postRender`**：在原有 **`marker-move`** 之外，对 **`activeMarkerEntities`** 循环重算 **`screen`** 并 **`marker-group-move`**。
- **StaticHome**：**`activeGroupPopups`** + 三个 handler；**`onMarkerGroupShow`** 时 **`activePopup = null`**；**`closePopup`** 仍只关单点；**`marker-popup--group`** 略小略紧、**`z-index: 99`**。
- **镜头**：仅 group 分支 **`flyCameraToUnitIdsBoundingSphere`** 使用 **`rangeScale: 1.6`**、**`minRange: 10000`**、**`pitchDeg: -30`**。

**未要求**：自动化测试执行说明。
